"use strict";
{
    // array
    let friend = ['reach', 'ssjoy'];
    friend.push('sultan');
    let eligibleRollList = [1, 3, 5];
    let coordinates = [1, 6];
    let ageName = [1, 'joy'];
    let samiName = [1, 'joy', true];
}
